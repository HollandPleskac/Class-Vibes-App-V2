import 'package:flutter/material.dart';

class ServersDown extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Servers are Down'),
      ),
    );
  }
}
