import 'package:flutter/material.dart';

class DoingGreatTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('viewing students doing great'),
    );
  }
}